# LinkedIn Credentials
credentials:
  linkedin:
    email: saianantula007@outlook.com
    password: Saikowshik@2000
  openai:
    api_key: your_openai_api_key

# API Configuration
api:
  level: INFO
  host: 0.0.0.0
  port: 8000
  jobtrack_url: http://jobtrak-api:8000

# Selenium Configuration
selenium:
  remote_url: http://selenium-chrome:4444/wd/hub
  headless: true

# Database Configuration
database:
  path: postgresql://postgres:postgres@infra-postgres:5432/postgres

# Cache Configuration
cache:
  job_cache_size: 1000
  search_cache_size: 100

# Model Configuration
model:
  provider: openai
  name: gpt-4.1-mini
  temperature: 0.3

# Project Paths
paths:
  project: /app
  resume: resume/resume.yaml
  data: output/data
  resources: resources
  prompts: prompts
  config: config
  output_dir: output
  background_tasks_log: data/background_tasks/tasks.log

# Default Files
files:
  default_resume: resume/resume.yaml
  prompts_yaml: prompts/prompts.yaml
  descriptions_yaml: prompts/extractor_descriptions.yaml

# Application Settings
settings:
  max_concurrent_workers: 4
  max_retries: 3
  backoff_factor: 5
  open_file_command: notepad

# Request Headers
request_headers:
  user_agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/70.0.3538.102 Safari/537.36 Edge/18.19582